diffusers==0.34.0
transformers==4.48.1
torch==2.7.1
mediapy
wandb
swanlab
accelerate
tqdm
numpy
decord
einops
scipy
pandas
# ABC-130k (LeRobot v3) data path
pyarrow
huggingface_hub
av
# the original MCAP release, which abc130k reads; `pip install ./abc130k` pulls these too
mcap
mcap-protobuf-support
lpips
torchvision
# point tracking for clipeval; install on a login node, Leonardo compute nodes have no network
